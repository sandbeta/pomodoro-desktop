# 🗺️ 产品路线图

> 角色视角：产品经理 · 更新于 2026-09-26
> 产品定位：**温馨治愈的本地优先番茄钟** —— 陪伴感、隐私、开源是三大资产。
> 规划原则：强化定位、克制扩张。不做待办全家桶，不做云账号体系。
> 需要跨设备时，让用户把已经属于他的那个文件放进自己的同步目录 —— 而不是我们替他保管。

## ✅ v0.1（已发布）

- 三阶段自动流转的计时引擎（结束时间戳计时，无漂移）
- 治愈视觉：阶段色温 / 呼吸动画 / 圆环进度 / 番茄计数
- 专注统计：今日/本周/本月 + 近 7 天趋势图
- 本地持久化（electron-store，数据不出机器）
- Windows 分发：便携版 + NSIS 安装包，自绘番茄图标
- GPU 自适应渲染（默认硬件加速，崩溃自动降级保底）

## ✅ v0.2（已发布）· 托盘 / 通知 / 设置 / 导出

| # | 功能 | 说明 |
| --- | --- | --- |
| 1 | 托盘常驻 | 关闭窗口进托盘（可配），左键显隐，菜单退出；最小化后计时不失准（关后台节流） |
| 2 | 系统通知 | 阶段结束弹原生通知，点击唤回窗口；设置可关 |
| 3 | 设置面板 | 三档时长、长休节奏、自动开始、声音、通知、关窗行为；主进程校验后持久化 |
| 4 | 数据导出与清空 | 历史记录导出 JSON/CSV（系统保存对话框），一键清空（二次确认） |

## ✅ v0.3（开发中）· 已交付

| 功能 | 说明 |
| --- | --- |
| **中断记录** | 「标记中断」落一条 `completed:false` + `elapsedSec` 的记录，统计回答「你通常倒在第几分钟」。中断不计入番茄数 |
| **数据可迁移** | 历史文件可改存到任意目录（Dropbox / iCloud / Syncthing），多设备共用同一份；新增导入与合并去重 |
| **网页版在线试用** | `docs/demo/` 用一层 electronAPI 桩把渲染层跑在浏览器里，数据落 localStorage。落地页 `docs/` 兼作项目主页 |

## 📋 v0.3 候选 · 差异化与留存 P1

| # | 功能 | 理由 | 风险提示 |
| --- | --- | --- | --- |
| 6 | 每日目标 + **累积型**花园 | 一年格子 / 只增不减的视觉产物。原方案是 streak 连续天数，已改向 —— 见下方注 | 需要中断数据垫底，否则格子只能画「你有多棒」 |
| 7 | 治愈白噪音 | 雨声/篝火/咖啡馆，Web Audio **合成**而非打包音频（延续架构决策：仓库干净、无版权） | 长循环合成的听感需实测；差异化在混音器（分轨音量/睡眠定时/交叉淡入）而非音色本身，做不出混音器就不如只做一种声音。CPU 增量要监控 |
| 9 | 周报分享图 | 从 v1.0 **提前**：它是清单里唯一以「拉新」为主要职责的功能。有了中断数据才有可转发的句子（「你 40% 的番茄倒在 18 分钟」），而不是「本周 8.8 小时」 | canvas 渲染，无外部依赖 |
| 8 | 多主题配色包 | 抹茶/日落/夜航星 | **原估「成本极低」不成立**：`:root` 只有 5 个文字/描边 token，三档阶段色是硬编码在 `.stage.mode-*` 里的十六进制，得先把阶段色系统重构成 token 才谈得上换肤 |

> **注：为什么删掉了「任务标签」（原 #5）**
> 原文称它是「从计时器跨入生产力工具的分水岭」——这个措辞本身就是危险信号。它意味着一个单人维护的开源仓库要去正面竞争 Todoist / Things 的待办模块，而本文件底部的「不做」清单明确写了「不做完整待办管理」。何况它是这个品类里最同质化的功能。
> 更实际的原因：加 `task` 字段会改动数据模型，而历史记录现在已经是**用户自己保管的文件**，格式即公开契约，改一次断一次。

> **注：为什么把「连续天数」换成累积型产物**
> streak 是惩罚形状的机制，和本产品的治愈调性天然对冲 —— 原文自己也预感到了（「失败断签的文案要治愈不焦虑」）。需要靠文案去缓和的机制，说明机制本身有问题。只增不减的格子/花园同样能撑留存，且不需要道歉。

## 🔭 v1.0 候选 · 生态 P2

| # | 功能 | 说明 |
| --- | --- | --- |
| 10 | macOS / Linux 版 | Electron 天然跨平台，成本低，开源社区呼声 |
| 11 | 国际化 i18n | 目标全球开源用户。**但注意**：本产品的文案就是产品本身，「做得漂亮，起来喝口茶吧」的治愈感是中文特有的；机翻英文版是负资产。i18n 要么配真人翻译，要么不如不出 |
| 13 | GitHub Actions 自动发布 | 打 tag 自动出安装包到 Releases。顺带解决一个现实问题：82MB 的 exe 目前只能人工拖进 Release 页面 |

> 原 v1.0 的 **#9 周报分享图**与 **#12 全局快捷键**已提前进 v0.3 候选。快捷键的理由：习惯回路里最贵的摩擦是「打开应用 → 点开始」这两步，而它是每用户每天触发次数最高的功能，在 Electron 里也只是一个 API。

## 🚫 明确不做

- **网站屏蔽/强制锁定** —— 系统权限敏感，与温柔陪伴人设冲突
- **云同步/账号体系** —— 直接背叛本地优先卖点；确有需求给「指向用户自己的 WebDAV」类无账号方案
- **完整待办/项目管理** —— 那是另一个产品，任务标签（#5）是能力边界

## 📏 衡量指标

- 北极星：**人均每日完成番茄数**
- 形态验证：托盘模式使用占比（窗口常开 vs 隐藏计时）
- 留存：7 日回访率
- 开源健康度：star / issue 响应时长 / 外部贡献者数
- 性能红线：运行态总 CPU < 10%（单核折算），内存 < 300MB

## 里程碑映射（与 PROJECT-HANDOFF 对应）

- v0.2 ≈ M5 扩展版（托盘/通知/设置 + 导出）
- v0.3 ≈ M7（中断记录 + 数据可迁移 + 网页试用已交付；余下：累积花园/白噪音/周报图/快捷键）
- v1.0 ≈ M8（跨平台 + i18n + CI 发布）
